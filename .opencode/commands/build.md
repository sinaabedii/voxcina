---
description: Deploy the new codeson vps-ir VPS
---
Commit the changes and push the last commit, then pull the last commit on vps-ir VPS. 
Copy the files to existing docker container in vps-ir VPS and build them and deploy last commit.
